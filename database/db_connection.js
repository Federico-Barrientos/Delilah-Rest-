const db_user = "root";
const db_password = "";
const db_host = "localhost";
const db_port = "3306";
const db_name = "delilah_resto";

module.exports = {
    db_user,
    db_password,
    db_host,
    db_port,
    db_name
};